import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'IsoMTO — Isometric Drawing to MTO Generator',
  description:
    'Upload a piping isometric drawing and get a structured Material Take-Off: pipe, fittings, flanges, valves, gaskets, and bolt sets.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
